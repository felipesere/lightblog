<% content_for :body do %>
  <header class="hero">
    <nav>
      <%= erb :logo, locals: {logo: :blue_line_white_logo} %>
    </nav>

    <h1>Code Paradoxon</h1>
    <h2>Musings on Code</h2>
  </header>
  <div class="posts">
    <div class="content-container">
      <div class="container">
        <ul>
          <% posts.each do |post| %>
            <%= erb :post_snippet, :locals => {post: post} %>
          <% end %>
        </ul>

        <div class="bottom-navigation">
          <a href="#" class="right">Older Posts →</a>
        </div>
      </div>
    </div>

    <%= erb :footer %>
<% end %>

